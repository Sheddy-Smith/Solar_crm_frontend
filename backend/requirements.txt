# Pinned to the exact versions the app is developed and tested against.
# Floor-only pins (>=) let every Render rebuild silently pull new majors,
# which is how prod ended up on library behavior never tested locally
# (2026-07-11 outage). Upgrade deliberately: bump here, test, then deploy.
Django==4.2.30
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-environ==0.14.0
psycopg2-binary==2.9.12
redis==8.0.0
django-redis==6.0.0
Pillow==12.2.0
django-filter==25.1
gunicorn==26.0.0
whitenoise==6.12.0
django-storages==1.14.6
boto3==1.43.45
cryptography==49.0.0
